#include "main.h"

/**
 * _abs - prints absolute value
 * @num: integer to be tested
 * Return: 0
 */
int _abs(int num){
     if (num > 0)
	return (num);
     if (num < 0)
	return (-num);
     return (0);
}
